# Streamlit Cloud / light VPS profile — NO PySpark (Java + RAM killer on free tier)
# App already soft-falls: Spark only appears in engine picker if installed.

streamlit>=1.28
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
plotly>=5.18
python-dotenv>=1.0
pymodbus>=3.5
optuna>=3.0
google-generativeai>=0.8
openpyxl>=3.1
pyyaml>=6.0
requests>=2.31
matplotlib>=3.7
seaborn>=0.13
xgboost>=2.0
statsmodels>=0.14
llama-index-core>=0.10

# Mid-weight (usually OK on Cloud Community)
polars>=0.20
prophet>=1.1.5
cleanlab>=2.5
duckdb>=0.9

# Skip on free Cloud (install locally / Docker full image instead):
# pyspark, great-expectations, ydata-profiling, lightgbm, fastapi, uvicorn
